
const SavingOverlay = () => {
    return (
        <div className="text-center">
            <p className="text-primary text-lg font-semibold">Saved!</p>
        </div>

    );
};

export default SavingOverlay;
